\section{Preliminaries}
\label{sec:preliminaries}

\TODO{Compact definitions and standard lemmas, drawn from
\path{docs/preprint_outline.md} Section~2.}

\subsection{Cubic graphs and $\Sphere$-flows}
\label{sec:prelim-flows}

\TODO{Formal definitions of cubic, bridgeless, $A$-flow, nowhere-zero
$k$-flow, $\Sphere$-flow. Sign convention at each vertex.}

\begin{definition}[$\Sphere$-flow]
\label{def:s2-flow}
\TODO{Formal definition matching \cref{eq:kirchhoff} from
\cref{sec:intro-problem}.}
\end{definition}

\begin{lemma}[120-degree triple, after~{\cite[Observation~9]{HMM26}}]
\label{lem:120-degree}
\TODO{For unit vectors $v_1, v_2, v_3 \in \R^3$, $\sum_i v_i = 0$ iff
$\{0, v_1, v_2, v_3\}$ is coplanar and every pair of $v_i$'s makes
angle $2\pi/3$.}
\end{lemma}

\subsection{Snarks and nontrivial snarks}
\label{sec:prelim-snarks}

\TODO{Standard definitions of snark and \emph{nontrivial} snark
(girth $\ge 5$, cyclically $\ge 4$-edge-connected, $\chi' = 4$).
Cite~\cite{BGHM13} for the catalogue.}

\subsection{Cycle double covers and $H_d$-flows}
\label{sec:prelim-cdc}

\TODO{CDC, oriented CDC, $H_d$-flow. The MMRT theorem at $d = 4$.
Reference~\cite{MMRT25}.}

\subsection{The pinning gauge}
\label{sec:prelim-gauge}

\TODO{The $\SO(3)$ symmetry. Fixing it by pinning vertex 0's three
spokes to the canonical 120-degree triple in the $xy$-plane.
Drop-redundant-vertex to reach a square polynomial system; the rank
of the linear redundancy of the Kirchhoff equations on a connected
cubic graph is exactly~$3$.}

\begin{lemma}[Square pinned system]
\label{lem:square-system}
\TODO{On a connected cubic graph with $|E|$ edges and $|V|$ vertices,
the vertex Kirchhoff equations carry exactly $3$ linear dependencies.
After pinning vertex~$0$ and dropping one redundant Kirchhoff
equation, the remaining polynomial system has $|E| - 3$ unknowns and
$|E| - 3$ equations, in degree at most~$2$.}
\end{lemma}
