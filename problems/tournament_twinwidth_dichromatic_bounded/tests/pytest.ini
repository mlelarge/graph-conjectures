[pytest]
markers =
    slow: exact heavy computations (e.g. omegaVec(S_4) lower-bound exhaustion at n=15)
# default run skips slow tests; run them with:  pytest -m slow  or  pytest -m ''
addopts = -m "not slow"
